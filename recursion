def recursion(num):
    if num < 0:
        return 'invalid value'
    elif num == 1:
        return 1
    else:
        return num*(recursion(num-1))


if __name__ == '__main__':
    result = recursion(8)
    print(result)
